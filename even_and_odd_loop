#include <stdio.h>
int main()
{
    for (int i=0;i<=50;i++)
    {
        if (i%2==0)
        {
            printf("even : %d\n",i);
        }
        else
        {
            printf("odd : %d\n",i);
        }
    }
return 0;
}
